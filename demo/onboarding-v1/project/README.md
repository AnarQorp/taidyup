# Bundled onboarding project

This directory is intentionally minimal. The declaration is composed with the adjacent exported workflow and sanitized runtime JSONL by the real local application path. It contacts no provider and performs no action.
